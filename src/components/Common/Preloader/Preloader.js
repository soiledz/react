import React from "react";
import preloader from "../../../aces/image/preloader.svg";

let Preloader = (props) => {
    return <div>
        <img src={preloader} alt="preloader"/>
    </div>
}

export default Preloader;